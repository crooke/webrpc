{{define "new_client"}}
{{- if .Services}}
// TODO: get it from an element (or use window.origin?)
const hostname = "http://localhost:3000";
{{ range .Services}}
// TODO: more than one service name
let service = new {{.Name}}(hostname, window.fetch.bind());
{{end -}}
{{end -}}
{{end}}